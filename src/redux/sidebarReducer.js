
let initialState = {
    friends: [
        { id: 1, friend: 'Sasha' },
        { id: 2, friend: 'Anna' },
        { id: 3, friend: 'Kate' }
    ]
}

export const sidebarReducer = (state = initialState, action) => {
    return state
}

